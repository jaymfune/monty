#include "monty.h"

int main()
{
    Stack myStack;
    initialize(&myStack);

    /* Example usage of push and pall */
    push(&myStack, 10);
    push(&myStack, 20);
    push(&myStack, 30);

    pall(&myStack);

    /* pint */
    stack_t *myStack = NULL;
    bus = malloc(sizeof(stack_t)); // Allocate memory for bus

    pint(&myStack, 42);
    free(bus)

        return 0;
}
